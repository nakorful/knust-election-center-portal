import styled from 'styled-components';

export const RegisterIDStyles = styled.main`
  width: 100vw;
  min-height: calc(100vh - 200px);
  font-weight: normal;
  text-align: center;
  padding-top: 70px;
  
  a {
    margin-top: 12px;
  }
`
